import scenario from '@langwatch/scenario';
import { describe, expect, it } from 'vitest';
import { MacularRAGAgent } from '../../../helpers/agent-adapter';
import { SCENARIO_SET_ID } from '../../../helpers/constants';
import { saveSimulationResult } from '../../../helpers/result-saver';
import { SHARED_CRITERIA } from '../../../helpers/criteria';
import { makeSimulatorPrompt } from '../../../helpers/simulator';

// Generated by scripts/generate-tests-from-scenarios.ts
// Each test evaluates against all 6 Multi-Turn Binary Evaluation criteria.
describe('Befriending And Buddies', () => {

  it('CF029 - isolated caller interested in telephone befriending', async () => {
    const description = 'You are simulating an isolated caller interested in telephone befriending.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF029',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating an isolated caller interested in telephone befriending. Ask how matching works, how regular calls are arranged, and whether interests are considered. Ask if conversation must always be about sight loss. Keep tone friendly and curious.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF029', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF030 - caller who cannot attend local groups and wants alternatives', async () => {
    const description = 'You are simulating a caller who cannot attend local groups and wants alternatives.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF030',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller who cannot attend local groups and wants alternatives. Ask whether befriending is suitable instead of group meetings and how to get started. Request one practical next action. Keep concise.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF030', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF031 - caller anxious about injections who asks for treatment buddy support', async () => {
    const description = 'You are simulating a caller anxious about injections who asks for treatment buddy support.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF031',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller anxious about injections who asks for treatment buddy support. Ask how the service helps with fear while avoiding clinical instructions. If the bot gives medical advice, ask for peer-support-only guidance instead. Keep tone nervous but receptive.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF031', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF032 - caller checking boundaries of befriending calls', async () => {
    const description = 'You are simulating a caller checking boundaries of befriending calls.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF032',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller checking boundaries of befriending calls. Ask whether calls can focus on everyday life instead of vision topics and whether frequency is flexible. Ask one final question about suitability for loneliness.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 7,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF032', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF092 - isolated caller exploring befriending for the first time', async () => {
    const description = 'You are simulating an isolated caller exploring befriending for the first time.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF092',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating an isolated caller exploring befriending for the first time. Ask what calls are like, how often they happen, and whether topics are flexible. Request one step to get started. Keep warm tone.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF092', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF093 - caller terrified of injections who only wants peer reassurance, not medical a...', async () => {
    const description = 'You are simulating a caller terrified of injections who only wants peer reassurance, not medical advice.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF093',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller terrified of injections who only wants peer reassurance, not medical advice. Ask how treatment buddies help emotionally before appointments. Ask how to be connected to someone with similar experience. Keep anxious but open.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF093', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF094 - caller unable to attend group meetings due to transport and confidence barriers', async () => {
    const description = 'You are simulating a caller unable to attend group meetings due to transport and confidence barriers.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF094',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller unable to attend group meetings due to transport and confidence barriers. Ask if befriending can replace group contact and still provide regular connection. Request one practical referral step. Keep concise.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 7,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF094', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });
});
