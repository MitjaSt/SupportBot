import scenario from '@langwatch/scenario';
import { describe, expect, it } from 'vitest';
import { RAGAgent } from '../../../helpers/agent-adapter';
import { SCENARIO_SET_ID } from '../../../helpers/constants';
import { SHARED_CRITERIA } from '../../../helpers/criteria';
import { saveSimulationResult } from '../../../helpers/result-saver';
import { makeSimulatorPrompt } from '../../../helpers/simulator';

// Generated by scripts/generate-tests-from-scenarios.ts
// Each test evaluates against all 6 Multi-Turn Binary Evaluation criteria.
describe('Newly Diagnosed Support', () => {

  it('CF033 - newly diagnosed caller in shock asking what to do first', async () => {
    const description = 'You are simulating a newly diagnosed caller in shock asking what to do first.';
    const agent = new RAGAgent();

    const result = await scenario.run({
      name: 'CF033',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a newly diagnosed caller in shock asking what to do first. Request one simple first step today, then ask for the next step only after receiving the first. Keep messages short and overwhelmed.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF033', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF034 - caller asking if diagnosis means complete blindness soon', async () => {
    const description = 'You are simulating a caller asking if diagnosis means complete blindness soon.';
    const agent = new RAGAgent();

    const result = await scenario.run({
      name: 'CF034',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller asking if diagnosis means complete blindness soon. Ask whether independence and normal life are still possible. If responses are long, ask for plain language reassurance and one action. Keep anxious tone.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF034', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF035 - caller struggling to tell family and colleagues about macular disease', async () => {
    const description = 'You are simulating a caller struggling to tell family and colleagues about macular disease.';
    const agent = new RAGAgent();

    const result = await scenario.run({
      name: 'CF035',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller struggling to tell family and colleagues about macular disease. Ask for practical wording and emotional coping tips for that conversation. Request one short script for starting the discussion. Keep tone hesitant.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF035', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF036 - caller who worries reading and screens are damaging their eyes', async () => {
    const description = 'You are simulating a caller who worries reading and screens are damaging their eyes.';
    const agent = new RAGAgent();

    const result = await scenario.run({
      name: 'CF036',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller who worries reading and screens are damaging their eyes. Ask directly if screen time worsens macular disease, then ask what practical adjustments help. Keep language simple and focused.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 7,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF036', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF037 - caller asking for a practical first-week checklist after diagnosis', async () => {
    const description = 'You are simulating a caller asking for a practical first-week checklist after diagnosis.';
    const agent = new RAGAgent();

    const result = await scenario.run({
      name: 'CF037',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller asking for a practical first-week checklist after diagnosis. Ask for support, admin, and information steps in sensible order without medical treatment directives. If too much detail appears, ask for top three actions only.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF037', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF038 - caller who asks for changing depth explanations', async () => {
    const description = 'You are simulating a caller who asks for changing depth explanations.';
    const agent = new RAGAgent();

    const result = await scenario.run({
      name: 'CF038',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller who asks for changing depth explanations. First ask for simple explanation of condition, then ask for more detail, then ask for simpler restatement again. Check if the bot adapts without drifting. Keep tone cooperative.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF038', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });
});
