import scenario from '@langwatch/scenario';
import { describe, expect, it } from 'vitest';
import { MacularRAGAgent } from '../../../helpers/agent-adapter';
import { SCENARIO_SET_ID } from '../../../helpers/constants';
import { saveSimulationResult } from '../../../helpers/result-saver';
import { SHARED_CRITERIA } from '../../../helpers/criteria';

// Generated by scripts/generate-tests-from-scenarios.ts
// Each test evaluates against all 6 Multi-Turn Binary Evaluation criteria.
describe('Working Age And Daily Life', () => {

  it('CF050 - 38-year-old employed caller worried about disclosing sight loss at work', async () => {
    const description = 'You are simulating a 38-year-old employed caller worried about disclosing sight loss at work.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF050',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a 38-year-old employed caller worried about disclosing sight loss at work. Ask how to approach employer conversation and what support exists for staying in work safely. Ask for one immediate next step. Keep practical tone. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF050', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF051 - working-age caller asking about Access to Work support', async () => {
    const description = 'You are simulating a working-age caller asking about Access to Work support.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF051',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a working-age caller asking about Access to Work support. Ask what types of help it can cover and how to begin. Request concise explanation without legal complexity. Keep businesslike tone. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF051', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF052 - caller uncertain about driving obligations', async () => {
    const description = 'You are simulating a caller uncertain about driving obligations.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF052',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller uncertain about driving obligations. Ask when DVLA notification is required and what to do if both eyes are affected. If response is vague, ask for one clear practical next step. Keep concise and worried. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF052', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF053 - 19-year-old with inherited macular condition and a concerned parent', async () => {
    const description = 'You are simulating a 19-year-old with inherited macular condition and a concerned parent.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF053',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a 19-year-old with inherited macular condition and a concerned parent. Ask what young-person support exists for education, social stress, and family guidance. Request support routes that are realistic and immediate. Keep tone vulnerable but clear. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF053', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF086 - working-age caller unsure how to tell employer about sight changes', async () => {
    const description = 'You are simulating a working-age caller unsure how to tell employer about sight changes.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF086',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a working-age caller unsure how to tell employer about sight changes. Ask for practical wording and immediate support route for job retention. Request concise next action. Keep tone practical. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF086', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF087 - caller asking what Access to Work can realistically fund for central vision loss', async () => {
    const description = 'You are simulating a caller asking what Access to Work can realistically fund for central vision loss.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF087',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller asking what Access to Work can realistically fund for central vision loss. Ask how to start the process and who to contact first. Keep concise and administrative. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF087', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF088 - parent of a teenager with inherited macular disease seeking support', async () => {
    const description = 'You are simulating a parent of a teenager with inherited macular disease seeking support.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF088',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a parent of a teenager with inherited macular disease seeking support. Ask what help exists for education stress, social confidence, and family coping. Request one immediate contact route. Keep worried but clear. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF088', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });
});
