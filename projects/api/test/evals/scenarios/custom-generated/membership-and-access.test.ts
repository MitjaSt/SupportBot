import scenario from '@langwatch/scenario';
import { describe, expect, it } from 'vitest';
import { MacularRAGAgent } from '../../../helpers/agent-adapter';
import { SCENARIO_SET_ID } from '../../../helpers/constants';
import { saveSimulationResult } from '../../../helpers/result-saver';
import { SHARED_CRITERIA } from '../../../helpers/criteria';
import { makeSimulatorPrompt } from '../../../helpers/simulator';

// Generated by scripts/generate-tests-from-scenarios.ts
// Each test evaluates against all 6 Multi-Turn Binary Evaluation criteria.
describe('Membership And Access', () => {

  it('CF059 - member with login and subscription confusion', async () => {
    const description = 'You are simulating a member with login and subscription confusion.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF059',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a member with login and subscription confusion. Ask how to access member-only content, what membership payment terms are after trial, and how to cancel if needed. Request one clear contact route for account help. Keep transactional tone.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 7,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF059', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF060 - referrer asking about consent and data privacy when referring someone else', async () => {
    const description = 'You are simulating a referrer asking about consent and data privacy when referring someone else.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF060',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a referrer asking about consent and data privacy when referring someone else. Ask what consent is required, what data is collected, and how communication preferences are handled. Request one concise checklist before submitting referral. Keep administrative tone.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF060', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF099 - member who cannot access members-only pages and is unsure about trial-to-paid...', async () => {
    const description = 'You are simulating a member who cannot access members-only pages and is unsure about trial-to-paid transition.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF099',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a member who cannot access members-only pages and is unsure about trial-to-paid transition. Ask where to get account help and how cancellation works. Request one concise support contact option. Keep transactional tone.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 7,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF099', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF100 - professional making referrals and confirming privacy and consent handling', async () => {
    const description = 'You are simulating a professional making referrals and confirming privacy and consent handling.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF100',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a professional making referrals and confirming privacy and consent handling. Ask what consent must be documented, what data fields are essential, and how contact preferences are respected. Request a short pre-submission checklist. Keep administrative tone.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF100', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });
});
