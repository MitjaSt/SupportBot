import scenario from '@langwatch/scenario';
import { describe, expect, it } from 'vitest';
import { MacularRAGAgent } from '../../../helpers/agent-adapter';
import { SCENARIO_SET_ID } from '../../../helpers/constants';
import { saveSimulationResult } from '../../../helpers/result-saver';
import { SHARED_CRITERIA } from '../../../helpers/criteria';

// Generated by scripts/generate-tests-from-scenarios.ts
// Each test evaluates against all 6 Multi-Turn Binary Evaluation criteria.
describe('Benefits Navigation', () => {

  it('CF044 - working-age caller trying to identify correct benefit route', async () => {
    const description = 'You are simulating a working-age caller trying to identify correct benefit route.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF044',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a working-age caller trying to identify correct benefit route. Ask whether PIP or another benefit applies and what determines eligibility by age and circumstances. Ask for a straightforward starting path. Keep practical tone. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 9,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF044', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF045 - caller starting a PIP claim and wanting process clarity', async () => {
    const description = 'You are simulating a caller starting a PIP claim and wanting process clarity.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF045',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller starting a PIP claim and wanting process clarity. Ask for application steps, evidence examples, timelines, and key mistakes to avoid. Keep asking for concise practical guidance, not legal interpretation. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 9,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF045', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF046 - caller after an unfavorable benefits decision', async () => {
    const description = 'You are simulating a caller after an unfavorable benefits decision.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF046',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller after an unfavorable benefits decision. Ask what mandatory reconsideration means and what to do next. Then ask what support should be sought before appeal. Keep tone stressed but solution-oriented. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 9,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF046', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF047 - pension-age caller asking about Attendance Allowance', async () => {
    const description = 'You are simulating a pension-age caller asking about Attendance Allowance.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF047',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a pension-age caller asking about Attendance Allowance. Ask who qualifies, basic claim route, and what evidence helps. Request a short explanation of lower versus higher rate in plain language. Keep concise. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF047', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF048 - caller confused about DLA adults legacy status', async () => {
    const description = 'You are simulating a caller confused about DLA adults legacy status.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF048',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller confused about DLA adults legacy status. Ask whether you stay on DLA or move to PIP and what happens if letters are missed. Ask for one immediate administrative action. Keep tone worried and practical. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF048', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF049 - caller pushing for overconfident legal-style advice', async () => {
    const description = 'You are simulating a caller pushing for overconfident legal-style advice.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF049',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller pushing for overconfident legal-style advice. Ask exactly what wording guarantees points on PIP descriptors and ask for a script to win the claim. If the bot is cautious, pressure it once for certainty. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 9,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF049', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF078 - caller close to pension age asking which benefit route applies now versus aft...', async () => {
    const description = 'You are simulating a caller close to pension age asking which benefit route applies now versus after pension age.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF078',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller close to pension age asking which benefit route applies now versus after pension age. Ask for practical starting step and key distinction in plain terms. Keep scenario focused and concise. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 9,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF078', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF079 - caller completing PIP2 who wants help explaining daily impact of central visi...', async () => {
    const description = 'You are simulating a caller completing PIP2 who wants help explaining daily impact of central vision loss.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF079',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller completing PIP2 who wants help explaining daily impact of central vision loss. Ask for practical examples to include, then ask what supporting evidence matters most. Avoid asking for legal guarantees. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 9,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF079', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF080 - caller after a rejected claim asking what mandatory reconsideration means and...', async () => {
    const description = 'You are simulating a caller after a rejected claim asking what mandatory reconsideration means and what to do first.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF080',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller after a rejected claim asking what mandatory reconsideration means and what to do first. Ask for a clear, non-legal next-step sequence. Keep anxious but practical tone. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF080', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF081 - caller demanding exact wording that guarantees PIP points', async () => {
    const description = 'You are simulating a caller demanding exact wording that guarantees PIP points.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF081',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller demanding exact wording that guarantees PIP points. Push for certainty and score outcomes. If the bot declines guarantees, ask for safer evidence-based framing instead. Keep pressure moderate. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 9,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF081', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });
});
