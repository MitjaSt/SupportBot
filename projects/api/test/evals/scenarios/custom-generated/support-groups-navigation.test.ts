import scenario from '@langwatch/scenario';
import { describe, expect, it } from 'vitest';
import { MacularRAGAgent } from '../../../helpers/agent-adapter';
import { SCENARIO_SET_ID } from '../../../helpers/constants';
import { saveSimulationResult } from '../../../helpers/result-saver';
import { SHARED_CRITERIA } from '../../../helpers/criteria';
import { makeSimulatorPrompt } from '../../../helpers/simulator';

// Generated by scripts/generate-tests-from-scenarios.ts
// Each test evaluates against all 6 Multi-Turn Binary Evaluation criteria.
describe('Support Groups Navigation', () => {

  it('CF054 - caller wanting to join a local support group but unsure where to start', async () => {
    const description = 'You are simulating a caller wanting to join a local support group but unsure where to start.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF054',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller wanting to join a local support group but unsure where to start. Ask for local options without giving exact location first. If needed, ask what the next contact step should be. Keep practical and concise.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF054', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF055 - caller asking for condition-specific online community options', async () => {
    const description = 'You are simulating a caller asking for condition-specific online community options.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF055',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller asking for condition-specific online community options. Ask what online groups exist and how to join safely. Request one recommendation path if unsure. Keep tone curious and pragmatic.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF055', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF056 - caller choosing between virtual and face-to-face support formats', async () => {
    const description = 'You are simulating a caller choosing between virtual and face-to-face support formats.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF056',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller choosing between virtual and face-to-face support formats. Ask what options exist and how to decide which fits your needs. Request one short summary of pros and next action. Keep friendly tone.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF056', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF089 - caller wanting local support groups but not ready to share postcode yet', async () => {
    const description = 'You are simulating a caller wanting local support groups but not ready to share postcode yet.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF089',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller wanting local support groups but not ready to share postcode yet. Ask what first step to take and how matching usually works. If needed, ask who to contact directly. Keep practical tone.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF089', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF090 - caller preferring online groups due to mobility limits', async () => {
    const description = 'You are simulating a caller preferring online groups due to mobility limits.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF090',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller preferring online groups due to mobility limits. Ask what condition-specific online options exist and how to join safely. Request one recommended starting path. Keep concise and cooperative.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF090', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF091 - caller comparing virtual versus in-person support formats', async () => {
    const description = 'You are simulating a caller comparing virtual versus in-person support formats.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF091',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: makeSimulatorPrompt(`You are simulating a caller comparing virtual versus in-person support formats. Ask which might suit anxiety and travel constraints better and what to try first. Keep tone thoughtful and practical.`),
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF091', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });
});
