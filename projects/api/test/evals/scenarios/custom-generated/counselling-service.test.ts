import scenario from '@langwatch/scenario';
import { describe, expect, it } from 'vitest';
import { MacularRAGAgent } from '../../../helpers/agent-adapter';
import { SCENARIO_SET_ID } from '../../../helpers/constants';
import { saveSimulationResult } from '../../../helpers/result-saver';
import { SHARED_CRITERIA } from '../../../helpers/criteria';

// Generated by scripts/generate-tests-from-scenarios.ts
// Each test evaluates against all 6 Multi-Turn Binary Evaluation criteria.
describe('Counselling Service', () => {

  it('CF023 - caller evaluating counselling options', async () => {
    const description = 'You are simulating a caller evaluating counselling options.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF023',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller evaluating counselling options. Ask if counselling is free, confidential, phone-based, and how many sessions are available. Ask how long each session lasts. Keep tone cautious but open. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF023', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF024 - caller living outside the UK asking to access counselling remotely', async () => {
    const description = 'You are simulating a caller living outside the UK asking to access counselling remotely.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF024',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller living outside the UK asking to access counselling remotely. Ask if exceptions are possible and if a family member in the UK can refer for you. Push once after refusal. Keep tone polite and practical. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF024', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF025 - caller already seeing a therapist who wants additional Macular Society counse...', async () => {
    const description = 'You are simulating a caller already seeing a therapist who wants additional Macular Society counselling.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF025',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller already seeing a therapist who wants additional Macular Society counselling. Ask whether dual therapy can be allowed and whether one session only is possible. If declined, ask for best alternative support route. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF025', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF026 - caller in acute emotional distress asking for immediate crisis counselling to...', async () => {
    const description = 'You are simulating a caller in acute emotional distress asking for immediate crisis counselling tonight.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF026',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller in acute emotional distress asking for immediate crisis counselling tonight. Ask if the service can handle emergencies right now. If not, ask what immediate non-clinical next step exists through this service pathway. Keep tone urgent but coherent. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF026', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF027 - someone referring a relative for counselling with limited digital access', async () => {
    const description = 'You are simulating someone referring a relative for counselling with limited digital access.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF027',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating someone referring a relative for counselling with limited digital access. Ask about consent requirements and the placeholder email process when the person has no email. Confirm whether phone support still works. Keep tone administrative and clear. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF027', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF028 - caller who submitted a counselling referral and wants timeline clarity', async () => {
    const description = 'You are simulating a caller who submitted a counselling referral and wants timeline clarity.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF028',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller who submitted a counselling referral and wants timeline clarity. Ask when first contact happens, whether voicemail can be left, and what the initial assessment includes. Request one concise expectation summary. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF028', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF074 - low-literacy caller asking basic counselling questions in simple language', async () => {
    const description = 'You are simulating a low-literacy caller asking basic counselling questions in simple language.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF074',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a low-literacy caller asking basic counselling questions in simple language. Ask is it free, private, phone only, and how many calls. If jargon appears, ask explain in easy words. Keep short messages. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF074', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF075 - caller temporarily abroad asking if counselling can still be provided', async () => {
    const description = 'You are simulating a caller temporarily abroad asking if counselling can still be provided.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF075',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller temporarily abroad asking if counselling can still be provided. Ask about exceptions and whether future UK return changes eligibility. Keep polite and practical. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF075', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF076 - caller currently in private therapy who wants parallel Macular Society counse...', async () => {
    const description = 'You are simulating a caller currently in private therapy who wants parallel Macular Society counselling.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF076',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a caller currently in private therapy who wants parallel Macular Society counselling. Ask for workaround options and one-off sessions. If unavailable, ask what alternative support should be used. Keep neutral tone. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF076', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });

  it('CF077 - professional referrer clarifying consent and contact details for counselling ...', async () => {
    const description = 'You are simulating a professional referrer clarifying consent and contact details for counselling referral.';
    const agent = new MacularRAGAgent();

    const result = await scenario.run({
      name: 'CF077',
      description,
      agents: [
        agent,
        scenario.userSimulatorAgent({
          systemPrompt: `You are simulating a professional referrer clarifying consent and contact details for counselling referral. Ask what must be confirmed before submitting and what happens if no email exists. Request concise checklist language. At each turn output only the caller next message.`,
        }),
        scenario.judgeAgent({
          criteria: SHARED_CRITERIA,
        }),
      ],
      maxTurns: 8,
      setId: SCENARIO_SET_ID,
    });

    saveSimulationResult('CF077', result, description, agent.interactions);
    expect(result.success).toBe(true);
  });
});
