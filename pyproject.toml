[tool.ruff]
# Line length
line-length = 100

# Python version
target-version = "py311"

[tool.ruff.lint]
# Enable specific rule sets
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort (import sorting)
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
]

# Ignore specific rules
ignore = [
    "E501",  # Line too long (handled by formatter)
]

[tool.ruff.lint.isort]
# Import sorting configuration
known-first-party = ["shared"]
section-order = ["future", "standard-library", "third-party", "first-party", "local-folder"]

[tool.ruff.format]
# Use double quotes
quote-style = "double"

# Indent with spaces
indent-style = "space"

[tool.black]
line-length = 100
target-version = ['py311']

[tool.isort]
profile = "black"
line_length = 100

[tool.pytest.ini_options]
# Custom markers for test categorization
markers = [
    "agent_test: marks tests as agent simulation tests (may be slow)",
]
# Warning filters to suppress benign third-party warnings
filterwarnings = [
    # DeepEval asyncio deprecation warning (Python 3.10+ compatibility)
    "ignore:There is no current event loop:DeprecationWarning",
    # LangWatch local prompts.json warning (not using local prompts)
    "ignore:No prompts.json found:UserWarning",
]
